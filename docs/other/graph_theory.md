# 图论

基础概念

```md
有向，无向，权值
度，出度，入度
连通性：连通图（无向图），强连通图（有向图）， 连通分量（无向图），强连通分量（有向图）
图的构造： 朴素存储(自己起名)，邻接矩阵，邻接表
图的遍历：DFS, BFS

有向图，无向图， 边的权值
有多少边连接这个节点，就是这个节点，它有几度
出度和入度，都是针对有向图
任何一个节点，都可以达到所有的其余节点。连通性
连通分量？极大联通子图？

朴素存储
key value
1 2
1 4
2 3
3 2

邻接矩阵
n * n 二维矩阵 ， 10000个节点， 10000 * 10000
能快速判断两个节点是否相连 grid[3][5] = 0
其中1个节点只连接一个节点，浪费内存空间 ，其余9999 没用到
节点别那么大，边尽可能地多。因为已经为每个节点开辟了那么多的空间了。（选邻接矩阵）
稠密图（点少边多）

邻接表
适合节点很多，边很少，适合用邻接表
稀疏图（边少点多）

dfs深搜三部曲
void dfs(图，目前搜索的节点) {
    if(终止条件) {
        存放结果
        return;
    }
    for(选择本节点连接的节点) {
        处理节点
        dfs(图，选择的节点)
        回溯，撤销处理结果
    }
}


```